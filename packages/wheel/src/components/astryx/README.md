# Astryx component roadmap

The Markdown files in this directory are design drafts. Wheel does not export these components.

Implement one component family at a time. A family enters the public API after it has dedicated
source, unit tests for its behavior contract, a working catalog example, and browser tests for
keyboard and accessibility behavior. Do not restore the generated `createAstryxComponent`
wrappers.
