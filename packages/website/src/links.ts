/**
 * The landing page's view of the shared destination table. `home.mdx` imports
 * `./links`; the table itself lives with the shared header that renders it.
 */
export { LINKS, NAV, type LinkKey } from '../../docs/src/site/links';
