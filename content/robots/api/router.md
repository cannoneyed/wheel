# `wheel/router` API

> Generated from [`packages/wheel/src/router/index.ts`](../../../packages/wheel/src/router/index.ts). Do not edit this file by hand.

Import only from `wheel/router`. The linked declarations are the source of truth for signatures, members, defaults, and limits.

## `basedHistory`

Kind: function. Source: [packages/wheel/src/router/history.ts:170](../../../packages/wheel/src/router/history.ts#L170).

Mount an app under a path prefix (e.g. the demos app served at `/demos/` inside wheel.dev) without the route table knowing: the router keeps matching `/todos`, while the address bar shows `/demos/todos`. Reads strip the base; writes prepend it. A read of a path OUTSIDE the base passes through unchanged — the route table then treats it as not-found rather than mis-matching a stripped fragment.

## `basedHistoryOverride`

Kind: function. Source: [packages/wheel/src/router/router-service.ts:208](../../../packages/wheel/src/router/router-service.ts#L208).

A `ServiceProvider` override that mounts the router under a path prefix — the address bar shows `/demos/todos` while the route table matches `/todos`. Pass the deployment's base path (vite's `import.meta.env.BASE_URL`); a base of `/` or `''` leaves the default history untouched. <ServiceProvider overrides={[basedHistoryOverride(import.meta.env.BASE_URL)]}> <Root /> </ServiceProvider>

## `browserHistory`

Kind: function. Source: [packages/wheel/src/router/history.ts:72](../../../packages/wheel/src/router/history.ts#L72).

The real thing: `history.pushState` for navigation, `popstate` for the back and forward buttons, and a window-level write event for navigations made by OTHER router instances on the same page. The writing instance skips its own announcement (`writing` is set around the synchronous dispatch), so a router's navigations never echo back through its own `listen` — which is what keeps its URL atom from looping. `popstate` needs no such guard: the browser fires it only for user navigation.

## `buildUrl`

Kind: function. Source: [packages/wheel/src/router/match.ts:205](../../../packages/wheel/src/router/match.ts#L205).

Build the URL for a named route. The exact inverse of `matchUrl`. Throws for an unknown name or a missing param — both are programmer errors a call site can fix, unlike a bad URL from a user. Search values equal to their schema default are omitted, so a freshly-loaded page has a clean URL.

## `BuildUrlOptions`

Kind: interface. Source: [packages/wheel/src/router/match.ts:85](../../../packages/wheel/src/router/match.ts#L85).

Options accepted when building a URL for a route.

## `CompiledRoute`

Kind: interface. Source: [packages/wheel/src/router/match.ts:34](../../../packages/wheel/src/router/match.ts#L34).

One navigable route, flattened out of the tree with its full pattern resolved. The table holds these; matching is a scan over them.

## `compileRoutes`

Kind: function. Source: [packages/wheel/src/router/match.ts:117](../../../packages/wheel/src/router/match.ts#L117).

Compile a nested route literal into a table. Throws at startup — not at navigation time — for a duplicate param name in one chain or a malformed `$` segment. Both are typos that would otherwise surface as a mysteriously empty param much later.

## `createRouter`

Kind: function. Source: [packages/wheel/src/router/create-router.tsx:77](../../../packages/wheel/src/router/create-router.tsx#L77).

Compile a route tree and bind a service class and components to it. Throws immediately on a malformed tree (a `$` with no name, one param name twice in a chain, a route name containing a dot) — startup is the cheapest place to find a typo'd route.

## `CreateRouterOptions`

Kind: interface. Source: [packages/wheel/src/router/create-router.tsx:37](../../../packages/wheel/src/router/create-router.tsx#L37).

Options for `createRouter`.

## `defaultHistory`

Kind: function. Source: [packages/wheel/src/router/history.ts:155](../../../packages/wheel/src/router/history.ts#L155).

Browser history when there is a `window`, memory history otherwise. The fallback is what lets a router-connected service instantiate under Node (unit tests, SSR) without a `window` guard at every call site.

## `defineRoutes`

Kind: function. Source: [packages/wheel/src/router/match.ts:106](../../../packages/wheel/src/router/match.ts#L106).

Declare a route tree. The `const` type parameter keeps every path string literal, which is what makes `navigate('team.issues', { params: { teamId } })` typecheck against the actual tree rather than `string`. Most apps do not call this directly — `createRouter(routes)` in `router-service.ts` calls it and hands back the service class and the table-bound components together.

## `HistoryUrl`

Kind: type. Source: [packages/wheel/src/router/history.ts:20](../../../packages/wheel/src/router/history.ts#L20).

A location as the router handles it: path, search, and hash — never the origin.

## `LinkComponent`

Kind: type. Source: [packages/wheel/src/router/router-link.tsx:57](../../../packages/wheel/src/router/router-link.tsx#L57).

The generated `<Link>`: generic over destination so each `to` types its own params.

## `LinkProps`

Kind: type. Source: [packages/wheel/src/router/router-link.tsx:33](../../../packages/wheel/src/router/router-link.tsx#L33).

Props for the typed `<Link>` a router generates.

## `matchUrl`

Kind: function. Source: [packages/wheel/src/router/match.ts:169](../../../packages/wheel/src/router/match.ts#L169).

Match a URL (absolute or path-relative) against the table. Returns `null` when nothing matches — the caller renders its not-found view. Never throws: URLs come from users. Ambiguity is resolved deterministically, deepest first: a longer chain beats a shorter one, then fewer path params (so `/teams/new` prefers a literal `new` route over `$teamId`), then declaration order.

## `memoryHistory`

Kind: function. Source: [packages/wheel/src/router/history.ts:115](../../../packages/wheel/src/router/history.ts#L115).

An array and an index. Everything is synchronous, so a test can navigate, go back, and assert in straight-line code with no waiting.

## `memoryHistoryOverride`

Kind: function. Source: [packages/wheel/src/router/router-service.ts:186](../../../packages/wheel/src/router/router-service.ts#L186).

A `ServiceProvider` override that points the router at an in-memory history. <ServiceProvider overrides={[memoryHistoryOverride(['/teams/a'])]}> <Root /> </ServiceProvider> Deterministic and synchronous: `back()` notifies before it returns, so a test asserts on the next line with nothing to wait for.

## `NavigateArgs`

Kind: type. Source: [packages/wheel/src/router/router-service.ts:92](../../../packages/wheel/src/router/router-service.ts#L92).

The options argument, required only when the route actually needs one. `navigate('home')` compiles; `navigate('team.issues')` does not, because that route's chain declares `$teamId`. Expressed as a rest tuple because an ordinary optional parameter cannot be conditionally required.

## `NavigateOptions`

Kind: type. Source: [packages/wheel/src/router/router-service.ts:76](../../../packages/wheel/src/router/router-service.ts#L76).

Everything a navigation may specify.

## `NotFoundComponent`

Kind: type. Source: [packages/wheel/src/router/router-root.tsx:35](../../../packages/wheel/src/router/router-root.tsx#L35).

What to render when the URL matches no route in the table.

## `Outlet`

Kind: function. Source: [packages/wheel/src/router/outlet.tsx:90](../../../packages/wheel/src/router/outlet.tsx#L90).

Renders the child route inside a layout component. function TeamLayout() { return ( <div use:componentRoot> <TeamHeader /> <Outlet /> </div> ); } Throws when used outside a router root, because a silent empty render is the hardest routing bug to track down.

## `PathParams`

Kind: type. Source: [packages/wheel/src/router/types.ts:75](../../../packages/wheel/src/router/types.ts#L75).

The params a single `path` fragment declares. `'teams/$teamId'` → `{ teamId: string }`; `'issues'` → no fields. Path params are always `string` — a route that wants a number parses it in the service that uses it, so the router never guesses at a URL's meaning.

## `RouteChildren`

Kind: interface. Source: [packages/wheel/src/router/types.ts:35](../../../packages/wheel/src/router/types.ts#L35).

The child map of a route node — keys are the route's short names.

## `RouteComponent`

Kind: type. Source: [packages/wheel/src/router/types.ts:32](../../../packages/wheel/src/router/types.ts#L32).

A route's component: an ordinary Solid component, no router-specific props.

## `RouteDef`

Kind: interface. Source: [packages/wheel/src/router/types.ts:47](../../../packages/wheel/src/router/types.ts#L47).

One node in the route tree. A node with `children` is a layout: its component renders `<Outlet/>` where the child should appear. A node without children is a leaf. The ROOT node is never itself a match target — declare an index child (`path: '/'`) if `/` should render something.

## `RouteErrorComponent`

Kind: type. Source: [packages/wheel/src/router/route-error.tsx:73](../../../packages/wheel/src/router/route-error.tsx#L73).

Rendered in place of a route node that threw.

## `RouteErrorInfo`

Kind: interface. Source: [packages/wheel/src/router/route-error.tsx:43](../../../packages/wheel/src/router/route-error.tsx#L43).

Where a route error was caught.

## `RouteErrorProps`

Kind: interface. Source: [packages/wheel/src/router/route-error.tsx:63](../../../packages/wheel/src/router/route-error.tsx#L63).

What a route-error fallback is handed.

## `RouteErrorReporter`

Kind: type. Source: [packages/wheel/src/router/route-error.tsx:60](../../../packages/wheel/src/router/route-error.tsx#L60).

Notified for every route error, before the fallback renders.

## `RouteMatch`

Kind: interface. Source: [packages/wheel/src/router/match.ts:67](../../../packages/wheel/src/router/match.ts#L67).

A URL that matched a route in the table.

## `RouteName`

Kind: type. Source: [packages/wheel/src/router/types.ts:88](../../../packages/wheel/src/router/types.ts#L88).

Every navigable name in a tree, as a dotted string union. A node with no children contributes just its own name (`` `${K}.${never}` `` collapses to `never`, which vanishes from the union). The root contributes nothing — it is a layout, not a destination.

## `RouteNeedsNoArgs`

Kind: type. Source: [packages/wheel/src/router/types.ts:175](../../../packages/wheel/src/router/types.ts#L175).

True when navigating to `Name` needs no arguments at all — no params and no required search fields. Lets `navigate('home')` typecheck without an options object while `navigate('team.issues')` still demands `{ params: { teamId } }`.

## `RouteParams`

Kind: type. Source: [packages/wheel/src/router/types.ts:114](../../../packages/wheel/src/router/types.ts#L114).

The params required to navigate to `Name`, accumulated from the root down. `RouteParams<Root, 'team.issues'>` is `{ teamId: string }` when `team` declares `$teamId` — the child inherits its ancestors' params, so a call site cannot forget one.

## `Router`

Kind: interface. Source: [packages/wheel/src/router/create-router.tsx:57](../../../packages/wheel/src/router/create-router.tsx#L57).

Everything one route table produces: a service class and its bound components.

## `RouterHistory`

Kind: interface. Source: [packages/wheel/src/router/history.ts:23](../../../packages/wheel/src/router/history.ts#L23).

The seam between the router and the address bar.

## `RouterHistoryService`

Kind: class. Source: [packages/wheel/src/router/router-service.ts:171](../../../packages/wheel/src/router/router-service.ts#L171).

Holds the `RouterHistory` the router drives. Its own service so tests can swap in `memoryHistory()` through the ordinary override seam: <ServiceProvider overrides={[memoryHistoryOverride(['/teams/a'])]}>

## `RouterService`

Kind: class. Source: [packages/wheel/src/router/router-service.ts:225](../../../packages/wheel/src/router/router-service.ts#L225).

The router. Instantiate through `createRouter(routes)` rather than injecting this class directly — the generated subclass is what carries the route table and therefore the types.

## `RouteSearchInput`

Kind: type. Source: [packages/wheel/src/router/types.ts:158](../../../packages/wheel/src/router/types.ts#L158).

What a caller may PASS as search when navigating to `Name` — the merged schemas' input type, so fields with defaults stay optional.

## `RouteSearchOutput`

Kind: type. Source: [packages/wheel/src/router/types.ts:166](../../../packages/wheel/src/router/types.ts#L166).

What a matched route REPORTS as search for `Name` — the merged schemas' output type, so defaults are already applied and every field is present.

## `RouteTable`

Kind: interface. Source: [packages/wheel/src/router/match.ts:57](../../../packages/wheel/src/router/match.ts#L57).

A compiled route table. Carries its literal tree as a phantom type so `navigate` and `<Link>` can check names, params, and search against it.

## `SearchAtomOptions`

Kind: interface. Source: [packages/wheel/src/router/router-service.ts:143](../../../packages/wheel/src/router/router-service.ts#L143).

How a URL-bound atom writes its value to the address bar.

## `Segment`

Kind: type. Source: [packages/wheel/src/router/match.ts:26](../../../packages/wheel/src/router/match.ts#L26).

One parsed piece of a path pattern: fixed text or a `$name` capture.

## `TypedRouteMatch`

Kind: interface. Source: [packages/wheel/src/router/router-service.ts:114](../../../packages/wheel/src/router/router-service.ts#L114).

The active match seen from ONE route's point of view, with that route's params and search typed. Returned for any route in the active chain, not just the leaf — so a layout at `teams.detail` reads its own `teamId` while `teams.detail.issues` is what actually matched. That is the whole reason nested routes are usable without a `useParams()` hook.

## `UncheckedNavigateOptions`

Kind: interface. Source: [packages/wheel/src/router/router-service.ts:100](../../../packages/wheel/src/router/router-service.ts#L100).

Navigation options addressed by plain string, used inside the router's own components.
