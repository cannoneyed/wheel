# `wheel/kit` API

> Generated from [`packages/wheel/src/kit/index.ts`](../../../packages/wheel/src/kit/index.ts). Do not edit this file by hand.

Import only from `wheel/kit`. The linked declarations are the source of truth for signatures, members, defaults, and limits.

## `applyDockIntent`

Kind: function. Source: [packages/wheel/src/kit/layout/split-tree.ts:46](../../../packages/wheel/src/kit/layout/split-tree.ts#L46).

Apply one dock drop: detach the dragged panel, land it on the target panel's edge, and normalize. Same-axis drops insert a sibling; cross-axis drops wrap the target in a new split. Untouched branches keep their object identity so renderers can preserve their DOM. Impossible intents (unknown ids, self-drops) return the tree unchanged.

## `Command`

Kind: interface. Source: [packages/wheel/src/kit/command-palette.tsx:31](../../../packages/wheel/src/kit/command-palette.tsx#L31).

A registered command — pure data plus its action.

## `CommandGroup`

Kind: interface. Source: [packages/wheel/src/kit/command-palette.tsx:55](../../../packages/wheel/src/kit/command-palette.tsx#L55).

Ranked results cut into their headings — what the palette renders.

## `CommandPaletteService`

Kind: class. Source: [packages/wheel/src/kit/command-palette.tsx:92](../../../packages/wheel/src/kit/command-palette.tsx#L92).

Owns the command table, search ranking, and the palette's open state. Everything is headless: `commands()`/`search()` are computeds, `run(id)` invokes by id — the host component is just a viewer over this data.

## `CommandPaletteSystem`

Kind: function. Source: [packages/wheel/src/kit/command-palette.tsx:217](../../../packages/wheel/src/kit/command-palette.tsx#L217).

Mount once at the app root. Registers the toggle combos with KeyboardService while mounted and renders the palette overlay: scrim, query input, ranked results, arrow-key selection, Enter runs, Escape closes. Focus is captured on open and restored on close via FocusService.

## `ConfirmOptions`

Kind: interface. Source: [packages/wheel/src/kit/dialog.tsx:37](../../../packages/wheel/src/kit/dialog.tsx#L37).

Options for the confirm/alert built-ins.

## `connectCommandPaletteSystem`

Kind: value. Source: [packages/wheel/src/kit/command-palette.tsx:184](../../../packages/wheel/src/kit/command-palette.tsx#L184).

CommandPaletteSystem's connection — exported for stubs and the states file.

## `connectDialogSystem`

Kind: value. Source: [packages/wheel/src/kit/dialog-system.tsx:18](../../../packages/wheel/src/kit/dialog-system.tsx#L18).

DialogSystem's connection — exported for stubs and the states file.

## `contextMenu`

Kind: function. Source: [packages/wheel/src/kit/context-menu.tsx:143](../../../packages/wheel/src/kit/context-menu.tsx#L143).

The `use:contextMenu` directive — attach a menu to an existing element, no wrapper, no ref plumbing: <div use:contextMenu={{ id: `item:${props.id}`, menu: () => <ItemMenu id={props.id} /> }}> Re-registers reactively if the binding changes (e.g. id derived from props).

## `ContextMenu`

Kind: function. Source: [packages/wheel/src/kit/context-menu.tsx:186](../../../packages/wheel/src/kit/context-menu.tsx#L186).

Component-form fallback (directives only work on native elements): wraps the trigger surface in a plain div and binds it.

## `ContextMenuBinding`

Kind: interface. Source: [packages/wheel/src/kit/context-menu.tsx:42](../../../packages/wheel/src/kit/context-menu.tsx#L42).

What a trigger site declares: identity, lazy content, anchor mode.

## `ContextMenuService`

Kind: class. Source: [packages/wheel/src/kit/context-menu.tsx:76](../../../packages/wheel/src/kit/context-menu.tsx#L76).

Global menu awareness: which menu is open and where. Single-open is enforced by `open` being a scalar atom — opening one menu IS closing the previous one.

## `ContextMenuSystem`

Kind: function. Source: [packages/wheel/src/kit/context-menu.tsx:247](../../../packages/wheel/src/kit/context-menu.tsx#L247).

Mount once at the app root. Owns the portal, positioning, scrim, Escape, and focus restore for whatever menu is open.

## `createGesture`

Kind: function. Source: [packages/wheel/src/kit/layout/gesture-dom.ts:59](../../../packages/wheel/src/kit/layout/gesture-dom.ts#L59).

Bind the gesture machine to one element with Pointer Events. The adapter owns only side effects: pointer capture, listener lifetime, animation-frame move batching, and text-selection suppression during a drag. Every decision — slop, cancellation, modifiers, click-vs-drag — happens inside the machine, so the same torture cases covered by the machine's unit tests are the cases this element exhibits.

## `createGestureActor`

Kind: function. Source: [packages/wheel/src/kit/layout/gesture.ts:289](../../../packages/wheel/src/kit/layout/gesture.ts#L289).

Start one gesture actor with the given slop and callbacks.

## `CreateGestureOptions`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture-dom.ts:10](../../../packages/wheel/src/kit/layout/gesture-dom.ts#L10).

Configuration for one DOM-bound gesture.

## `createMenuStack`

Kind: function. Source: [packages/wheel/src/kit/menu-stack.ts:221](../../../packages/wheel/src/kit/menu-stack.ts#L221).

Build a stack over `root`. `onChange` fires after every state change.

## `Dialog`

Kind: function. Source: [packages/wheel/src/kit/dialog.tsx:195](../../../packages/wheel/src/kit/dialog.tsx#L195).

Declarative dialog registration — mounts nothing; captures its owner so `dialogService.open(id)` renders `content` with THIS site's context (scoped service overrides included): <Dialog id="delete-item" content={() => <DeleteItemDialog … />} />

## `DialogService`

Kind: class. Source: [packages/wheel/src/kit/dialog.tsx:57](../../../packages/wheel/src/kit/dialog.tsx#L57).

Owns the open dialog and the registration map. One dialog at a time by construction.

## `DialogSystem`

Kind: function. Source: [packages/wheel/src/kit/dialog-system.tsx:33](../../../packages/wheel/src/kit/dialog-system.tsx#L33).

The one dialog host. Mount once inside the provider.

## `DockEdge`

Kind: type. Source: [packages/wheel/src/kit/layout/split-tree.ts:22](../../../packages/wheel/src/kit/layout/split-tree.ts#L22).

Which side of the target panel a dropped panel lands on.

## `dockEdgeAt`

Kind: function. Source: [packages/wheel/src/kit/layout/dock.tsx:45](../../../packages/wheel/src/kit/layout/dock.tsx#L45).

The dock edge a pointer position selects inside a leaf rectangle.

## `DockIntent`

Kind: interface. Source: [packages/wheel/src/kit/layout/split-tree.ts:25](../../../packages/wheel/src/kit/layout/split-tree.ts#L25).

A completed dock drop, reported by `Frame.Dock` and applied by an app action.

## `flattenLeaves`

Kind: function. Source: [packages/wheel/src/kit/menu-stack.ts:196](../../../packages/wheel/src/kit/menu-stack.ts#L196).

Every ACTION in the tree, depth first, with the groups dropped. A filtered list holds leaves only. Offering "Heading" beside "Heading 1" would make the user choose twice for one outcome. A level that carries a grid or an input is a WIDGET, and the search does not enter it. Its items configure that widget — "Header row" belongs to the size picker, not to the catalogue, and reaching that setting from a filter would flip it for a picker the user is not looking at. The GROUP stands in for it instead, so `/table` still finds the picker in one step.

## `focusScope`

Kind: function. Source: [packages/wheel/src/kit/focus.ts:334](../../../packages/wheel/src/kit/focus.ts#L334).

The `use:focusScope` directive — declare a focus scope on an existing element, no wrapper, no ref plumbing: <div use:focusScope={{ id: 'board' }}>…</div> The binding may carry `data` — what the region is working on: <div use:focusScope={{ id: 'editor', data: { documentId: doc().id } }}> Registers the scope, keeps the service's active path current through focusin/focusout, and makes the element programmatically focusable (`tabindex="-1"`) so `FocusService.focus('board')` always lands. Only the scope ID re-registers; a data change is read through the accessor, because a re-registration would drop the scope out of the active path for a frame.

## `FocusScopeBinding`

Kind: interface. Source: [packages/wheel/src/kit/focus.ts:37](../../../packages/wheel/src/kit/focus.ts#L37).

What a `use:focusScope` site declares: the scope's identity, and its subject.

## `FocusService`

Kind: class. Source: [packages/wheel/src/kit/focus.ts:100](../../../packages/wheel/src/kit/focus.ts#L100).

Tracks which focus scopes contain the focused element and offers the capture/restore ritual overlays need. Scope registration comes from the `use:focusScope` directive (or `registerScope` directly in headless tests); the active path is plain reactive data, inspectable in the debug panel.

## `Frame`

Kind: value. Source: [packages/wheel/src/kit/layout/frame-namespace.ts:8](../../../packages/wheel/src/kit/layout/frame-namespace.ts#L8).

The framing component namespace: `Frame.Row`, `Frame.Column`, `Frame.Drawer`, and the opt-in `Frame.Dock`.

## `FrameAxis`

Kind: type. Source: [packages/wheel/src/kit/layout/model.ts:8](../../../packages/wheel/src/kit/layout/model.ts#L8).

Axis along which a split arranges its children.

## `FrameColumn`

Kind: function. Source: [packages/wheel/src/kit/layout/frame.tsx:808](../../../packages/wheel/src/kit/layout/frame.tsx#L808).

Split children top to bottom; resize handles appear between siblings.

## `FrameDock`

Kind: function. Source: [packages/wheel/src/kit/layout/dock.tsx:65](../../../packages/wheel/src/kit/layout/dock.tsx#L65).

Render an app-owned split tree and run its dock-drop interaction. `Frame.Dock` never mutates the tree: a completed drop surfaces as a `DockIntent` for an application action to apply. Leaves render through the `panel` render prop, splits render as ordinary `Frame.Row`/`Frame.Column` frames, so resizing, persistence, and observability work unchanged.

## `FrameDockLeaf`

Kind: interface. Source: [packages/wheel/src/kit/layout/dock.tsx:19](../../../packages/wheel/src/kit/layout/dock.tsx#L19).

One leaf handed to the panel render prop.

## `FrameDockPanelDrag`

Kind: interface. Source: [packages/wheel/src/kit/layout/dock.tsx:11](../../../packages/wheel/src/kit/layout/dock.tsx#L11).

Drag wiring handed to the panel render prop; attach `grip` to the drag affordance.

## `FrameDockProps`

Kind: interface. Source: [packages/wheel/src/kit/layout/dock.tsx:24](../../../packages/wheel/src/kit/layout/dock.tsx#L24).

Props for the opt-in free-form docking surface.

## `FrameDrawer`

Kind: function. Source: [packages/wheel/src/kit/layout/frame.tsx:813](../../../packages/wheel/src/kit/layout/frame.tsx#L813).

A pull-over overlay panel; sits above content instead of pushing it.

## `FrameDrawerProps`

Kind: interface. Source: [packages/wheel/src/kit/layout/frame.tsx:56](../../../packages/wheel/src/kit/layout/frame.tsx#L56).

Props for the pull-over overlay panel.

## `FrameKind`

Kind: type. Source: [packages/wheel/src/kit/layout/model.ts:5](../../../packages/wheel/src/kit/layout/model.ts#L5).

The three frame kinds; rows and columns split, drawers overlay.

## `FramePixels`

Kind: type. Source: [packages/wheel/src/kit/layout/frame.tsx:26](../../../packages/wheel/src/kit/layout/frame.tsx#L26).

A pixel length in its one accepted spelling: `'240px'`.

## `FrameRegistrationInput`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:17](../../../packages/wheel/src/kit/layout/model.ts#L17).

Everything a mounting frame component tells the service about itself.

## `FrameRow`

Kind: function. Source: [packages/wheel/src/kit/layout/frame.tsx:803](../../../packages/wheel/src/kit/layout/frame.tsx#L803).

Split children left to right; resize handles appear between siblings.

## `FrameSize`

Kind: type. Source: [packages/wheel/src/kit/layout/model.ts:2](../../../packages/wheel/src/kit/layout/model.ts#L2).

A track preference in the only two accepted spellings: `'240px'` or `'1fr'`.

## `FrameSplitProps`

Kind: interface. Source: [packages/wheel/src/kit/layout/frame.tsx:29](../../../packages/wheel/src/kit/layout/frame.tsx#L29).

Shared geometry props for `Frame.Row` and `Frame.Column`.

## `GestureActor`

Kind: type. Source: [packages/wheel/src/kit/layout/gesture.ts:286](../../../packages/wheel/src/kit/layout/gesture.ts#L286).

A running gesture actor; `send` events, read `getSnapshot().value`.

## `GestureCallbacks`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture.ts:22](../../../packages/wheel/src/kit/layout/gesture.ts#L22).

Semantic outputs of the gesture machine; every callback is optional.

## `GestureDelta`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture.ts:12](../../../packages/wheel/src/kit/layout/gesture.ts#L12).

One drag sample: displacement from the press origin, absolute position, and live modifiers.

## `GestureEvent`

Kind: type. Source: [packages/wheel/src/kit/layout/gesture.ts:45](../../../packages/wheel/src/kit/layout/gesture.ts#L45).

Events the DOM adapter (or a test script) feeds into the machine.

## `GestureHandle`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture-dom.ts:30](../../../packages/wheel/src/kit/layout/gesture-dom.ts#L30).

A bound gesture; dispose removes every listener and stops the actor.

## `GestureInput`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture.ts:38](../../../packages/wheel/src/kit/layout/gesture.ts#L38).

Static configuration for one gesture actor.

## `gestureMachine`

Kind: value. Source: [packages/wheel/src/kit/layout/gesture.ts:118](../../../packages/wheel/src/kit/layout/gesture.ts#L118).

The pure pointer-gesture statechart shared by every Wheel drag interaction. `idle → pressed → dragging` with explicit outcomes for every abnormal event: Escape, `pointercancel`, capture loss, a second pointer, and window blur all cancel a drag; releasing before the slop threshold is a click, never a drag. The machine owns every decision; DOM side effects live in the adapter.

## `GestureModifiers`

Kind: interface. Source: [packages/wheel/src/kit/layout/gesture.ts:4](../../../packages/wheel/src/kit/layout/gesture.ts#L4).

Modifier-key state carried with every gesture sample.

## `GridPoint`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:108](../../../packages/wheel/src/kit/menu-stack.ts#L108).

Where the highlight sits inside a level's grid.

## `groupCommands`

Kind: function. Source: [packages/wheel/src/kit/command-palette.tsx:66](../../../packages/wheel/src/kit/command-palette.tsx#L66).

Cut ranked results into groups, keeping rank order. A group takes the position of its best-ranked member, so typing never reorders the list out from under the selection.

## `KeyBinding`

Kind: interface. Source: [packages/wheel/src/kit/keyboard.tsx:28](../../../packages/wheel/src/kit/keyboard.tsx#L28).

A declarative shortcut registration.

## `KeyboardService`

Kind: class. Source: [packages/wheel/src/kit/keyboard.tsx:172](../../../packages/wheel/src/kit/keyboard.tsx#L172).

Owns the shortcut table and the matching logic. Bindings live in a reactive atom (they ARE data — auditable via `bindingsFor`); dispatch is a pure-ish action over that data, so scope precedence is testable without a DOM listener in the loop.

## `KeyboardSystem`

Kind: function. Source: [packages/wheel/src/kit/keyboard.tsx:263](../../../packages/wheel/src/kit/keyboard.tsx#L263).

Mount once at the app root: the single document keydown listener feeding `KeyboardService.dispatch`. Renders nothing.

## `LayoutDiagnostic`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:114](../../../packages/wheel/src/kit/layout/model.ts#L114).

Recoverable failure surfaced to apps and the debug graph.

## `LayoutInteraction`

Kind: type. Source: [packages/wheel/src/kit/layout/model.ts:111](../../../packages/wheel/src/kit/layout/model.ts#L111).

Current pointer interaction, excluded from persistence.

## `LayoutMeasurement`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:11](../../../packages/wheel/src/kit/layout/model.ts#L11).

Rounded content-box measurement reported by a mounted frame.

## `LayoutNode`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:41](../../../packages/wheel/src/kit/layout/model.ts#L41).

The live geometry record for one mounted frame.

## `LayoutNodeSnapshot`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:120](../../../packages/wheel/src/kit/layout/model.ts#L120).

Persisted deviation from JSX defaults for one frame id.

## `LayoutResizeDraft`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:73](../../../packages/wheel/src/kit/layout/model.ts#L73).

A resize transaction; drafts never enter persisted geometry.

## `LayoutService`

Kind: class. Source: [packages/wheel/src/kit/layout/layout-service.ts:41](../../../packages/wheel/src/kit/layout/layout-service.ts#L41).

The batteries-included owner of frame geometry. Mounted `Frame` components register themselves by id; applications read `node(id)` and call `open`/`close`/`toggle`/`resize`/`reset`. The service owns only geometry — sizes, open state, constraints, measurements, resize drafts, and persistence. Structure belongs to application JSX.

## `LayoutServiceOptions`

Kind: interface. Source: [packages/wheel/src/kit/layout/layout-service.ts:18](../../../packages/wheel/src/kit/layout/layout-service.ts#L18).

Optional configuration; the zero-config default persists to local storage.

## `LayoutSnapshot`

Kind: interface. Source: [packages/wheel/src/kit/layout/model.ts:128](../../../packages/wheel/src/kit/layout/model.ts#L128).

Versioned geometry-only persistence payload; plain JSON, nothing else.

## `LayoutStorage`

Kind: interface. Source: [packages/wheel/src/kit/layout/storage.ts:4](../../../packages/wheel/src/kit/layout/storage.ts#L4).

Synchronous persistence boundary used before the first frame render.

## `localLayoutStorage`

Kind: function. Source: [packages/wheel/src/kit/layout/storage.ts:39](../../../packages/wheel/src/kit/layout/storage.ts#L39).

Local-storage adapter with lazy browser resolution and an injectable test host.

## `matchesCombo`

Kind: function. Source: [packages/wheel/src/kit/keyboard.tsx:139](../../../packages/wheel/src/kit/keyboard.tsx#L139).

Whether a keydown event matches a parsed combo — key compared case-insensitively, modifiers exactly (`ctrl+k` does NOT match `ctrl+shift+k`).

## `memoryLayoutStorage`

Kind: function. Source: [packages/wheel/src/kit/layout/storage.ts:18](../../../packages/wheel/src/kit/layout/storage.ts#L18).

Deterministic in-memory storage for tests, SSR, and explicit ephemeral layouts.

## `MenuAction`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:21](../../../packages/wheel/src/kit/menu-stack.ts#L21).

A menu entry that runs an action.

## `MenuGrid`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:74](../../../packages/wheel/src/kit/menu-stack.ts#L74).

A size picker: a grid of empty squares the pointer sweeps to choose a count. Word, Google Docs, and Notion all insert a table this way, and it beats two number fields because the shape is the answer.

## `MenuGroup`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:50](../../../packages/wheel/src/kit/menu-stack.ts#L50).

A menu entry that opens a submenu.

## `MenuInput`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:88](../../../packages/wheel/src/kit/menu-stack.ts#L88).

A level that asks for a value: a link's address, a new name. The field takes the focus and the keys, so the items below it stay reachable with the pointer but stop answering the arrows.

## `MenuItem`

Kind: type. Source: [packages/wheel/src/kit/menu-stack.ts:67](../../../packages/wheel/src/kit/menu-stack.ts#L67).

One entry: it RUNS, or it PUSHES.

## `MenuLevel`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:97](../../../packages/wheel/src/kit/menu-stack.ts#L97).

One level of the stack: what it is called, and what it holds.

## `menuMatches`

Kind: function. Source: [packages/wheel/src/kit/menu-stack.ts:213](../../../packages/wheel/src/kit/menu-stack.ts#L213).

Entries matching `query` across the WHOLE tree, or the level's own.

## `MenuStack`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:139](../../../packages/wheel/src/kit/menu-stack.ts#L139).

The headless stack (see module doc).

## `MenuStackPanel`

Kind: function. Source: [packages/wheel/src/kit/menu-stack-panel.tsx:126](../../../packages/wheel/src/kit/menu-stack-panel.tsx#L126).

The stacked menu panel (see module doc).

## `MenuStackPanelProps`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack-panel.tsx:24](../../../packages/wheel/src/kit/menu-stack-panel.tsx#L24).

What the panel needs to draw one stack.

## `MenuStackState`

Kind: interface. Source: [packages/wheel/src/kit/menu-stack.ts:116](../../../packages/wheel/src/kit/menu-stack.ts#L116).

Everything a renderer needs to draw the menu.

## `NO_MODIFIERS`

Kind: value. Source: [packages/wheel/src/kit/layout/gesture.ts:84](../../../packages/wheel/src/kit/layout/gesture.ts#L84).

No modifiers pressed; the initial machine state.

## `normalizeSplitTree`

Kind: function. Source: [packages/wheel/src/kit/layout/split-tree.ts:75](../../../packages/wheel/src/kit/layout/split-tree.ts#L75).

Collapse single-child splits, drop empty splits, and merge a split into a same-axis parent. Returns null for an empty tree; returns the input reference when nothing changed.

## `OpenDialogEntry`

Kind: interface. Source: [packages/wheel/src/kit/dialog.tsx:26](../../../packages/wheel/src/kit/dialog.tsx#L26).

What the system host renders for the open dialog.

## `panelIds`

Kind: function. Source: [packages/wheel/src/kit/layout/split-tree.ts:34](../../../packages/wheel/src/kit/layout/split-tree.ts#L34).

Every panelId in the tree, in depth-first order.

## `parseCombo`

Kind: function. Source: [packages/wheel/src/kit/keyboard.tsx:92](../../../packages/wheel/src/kit/keyboard.tsx#L92).

Parse a `'mod+k'`-style combo into an exact modifier set. `mod` resolves to cmd on macOS and ctrl elsewhere (override `mac` for headless tests). Throws on combos with no non-modifier key — a modifier-only "shortcut" is always a registration bug.

## `ParsedCombo`

Kind: interface. Source: [packages/wheel/src/kit/keyboard.tsx:68](../../../packages/wheel/src/kit/keyboard.tsx#L68).

A parsed key combo: exact modifier set + lowercased `event.key`.

## `parseFrameSize`

Kind: function. Source: [packages/wheel/src/kit/layout/model.ts:136](../../../packages/wheel/src/kit/layout/model.ts#L136).

Parsed unit and value of a `FrameSize`, or null for an invalid spelling.

## `parseLayoutSnapshot`

Kind: function. Source: [packages/wheel/src/kit/layout/model.ts:147](../../../packages/wheel/src/kit/layout/model.ts#L147).

Parse one untrusted storage payload; null when the shape is not usable.

## `removePanel`

Kind: function. Source: [packages/wheel/src/kit/layout/split-tree.ts:64](../../../packages/wheel/src/kit/layout/split-tree.ts#L64).

Remove one panel; null when the tree becomes empty.

## `Scrollbar`

Kind: function. Source: [packages/wheel/src/kit/layout/scrollbar.tsx:75](../../../packages/wheel/src/kit/layout/scrollbar.tsx#L75).

A permanent, framework-drawn scrollbar bound to one scroll container. It tracks the container's metrics through scroll events plus resize and mutation observers on the container AND its children, so the thumb grows and shrinks as panes are added, removed, or resized. The thumb drags through the same gesture machine as every other Wheel drag (Escape puts the scroll position back), the track pages on press, and the keyboard scrolls a focused bar. It renders as an in-flow gutter: place it after (axis `x`) or beside (axis `y`) the scroll container in a flex parent and it takes real layout space while something overflows, disappearing entirely otherwise — `Frame`'s `scrollable` prop does exactly that wiring. Theme via CSS custom properties: `--wheel-scrollbar-size`, `--wheel-scrollbar-track`, `--wheel-scrollbar-thumb`, and `--wheel-scrollbar-thumb-active`.

## `ScrollbarProps`

Kind: interface. Source: [packages/wheel/src/kit/layout/scrollbar.tsx:45](../../../packages/wheel/src/kit/layout/scrollbar.tsx#L45).

Props for the framework-managed scrollbar.

## `ScrollMetrics`

Kind: interface. Source: [packages/wheel/src/kit/layout/scrollbar.tsx:11](../../../packages/wheel/src/kit/layout/scrollbar.tsx#L11).

Live scroll metrics along one axis of a container.

## `SplitTree`

Kind: type. Source: [packages/wheel/src/kit/layout/split-tree.ts:8](../../../packages/wheel/src/kit/layout/split-tree.ts#L8).

The app-owned workspace tree behind `Frame.Dock`. Plain JSON: it lives in an application atom, persists with application persistence, and stores only ids — components come from the app's render prop, never from here.

## `thumbGeometry`

Kind: function. Source: [packages/wheel/src/kit/layout/scrollbar.tsx:26](../../../packages/wheel/src/kit/layout/scrollbar.tsx#L26).

Thumb geometry from scroll metrics — pure, so the sizing rules are unit testable: thumb length is the visible fraction of the track (clamped to a usable minimum), thumb position is the scrolled fraction of what remains. Null when nothing overflows.

## `Toast`

Kind: interface. Source: [packages/wheel/src/kit/toast.tsx:31](../../../packages/wheel/src/kit/toast.tsx#L31).

One toast on the stack. `id` is stable per concern (e.g. a mutation id).

## `ToastKind`

Kind: type. Source: [packages/wheel/src/kit/toast.tsx:26](../../../packages/wheel/src/kit/toast.tsx#L26).

Toast visual flavor.

## `ToastService`

Kind: class. Source: [packages/wheel/src/kit/toast.tsx:55](../../../packages/wheel/src/kit/toast.tsx#L55).

Owns the toast stack and each toast's lifecycle timers.

## `ToastState`

Kind: type. Source: [packages/wheel/src/kit/toast.tsx:28](../../../packages/wheel/src/kit/toast.tsx#L28).

Toast lifecycle state (drives the enter/leave transitions).

## `ToastSystem`

Kind: function. Source: [packages/wheel/src/kit/toast.tsx:211](../../../packages/wheel/src/kit/toast.tsx#L211).

The one toast host. Mount once inside the provider. Renders the stack bottom-right; click a toast to dismiss it. Pass `renderToast` to replace the default look entirely (the host still owns positioning and the click-to- dismiss wrapper).

## `useFrameParent`

Kind: function. Source: [packages/wheel/src/kit/layout/frame.tsx:90](../../../packages/wheel/src/kit/layout/frame.tsx#L90).

The parent split a frame is registered under, or null at a root.
